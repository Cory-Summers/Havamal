#pragma once
#include <iostream>
#include <fstream>
#include <map>
#include <string>
/*
namespace Config {
  class Loader
  {
  public:
    static Loader& Get();
  private:
    static Loader Initialize(std::string path = "config.cfg");
    void ReadFile();
    Loader() {}
    std::fstream m_cfg_file;
    std::map < std::string, int> m_map;
  };
}
*/